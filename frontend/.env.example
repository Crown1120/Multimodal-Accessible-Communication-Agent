# Bridge 后端地址。默认 http://localhost:8000
VITE_BACKEND_URL=http://localhost:8000

# ===== 魔珐星云 Web SDK =====
#
# 推荐做法：把凭据配置在后端 .env（XINGYUN_APP_ID / XINGYUN_APP_SECRET），
# 前端通过 GET /api/config/public 在运行时获取。
# 这样凭据不会被打包进 dist 静态产物，轮换密钥也无需重新构建前端。
#
# 兼容旧配置：这里填了值就会优先使用构建期注入（注意：VITE_ 前缀的变量
# 会被 Vite 内联进 JS，等于把凭据写进公开产物，仅供本地调试使用）。
# 留空时自动改用后端运行时配置；两者都为空则使用本地降级数字人。
VITE_XINGYUN_APP_ID=
VITE_XINGYUN_APP_SECRET=
